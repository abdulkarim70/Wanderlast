import React from 'react';

const DestinationsCard = ({destination}) => {
    {imageUrl, price , destinationName, duration , country}=destination
    return (
        <div>
            
        </div>
    );
};

export default DestinationsCard;